import { CheckMail } from '../../../components/auth/check-email'

export const CheckEmailPage = () => {
  return (
    <div style={{ margin: 'auto', width: '100%' }}>
      <CheckMail email={'email'} />
    </div>
  )
}
